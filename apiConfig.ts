// apiConfig.ts

/**
 * Base URL for all backend API calls.
 * When deploying with a real backend, update this to your server's API endpoint.
 * For development, this might point to a proxy path.
 */
export const BASE_API_URL = '/api'; // Example: 'https://your-app-api.com/api'
